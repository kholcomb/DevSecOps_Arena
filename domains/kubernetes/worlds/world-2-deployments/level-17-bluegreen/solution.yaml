apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: arena
spec:
  selector:
    app: myapp
    version: green  # Fixed: Now points to GREEN (new version)
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
