🔍 Observation Hint:

Check the deployment status:
`kubectl get deployment web-app -n devsecops-arena`

Look at the rollout status:
`kubectl rollout status deployment/web-app -n devsecops-arena`

What do you notice about the READY column and the rollout status?
