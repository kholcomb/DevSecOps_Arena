🔍 Observation Hint:

First, check if the pod is running: `kubectl get pod backend-app -n devsecops-arena`

Then check the service: `kubectl get service backend-service -n devsecops-arena`

Finally, check if the service found any pods: `kubectl get endpoints backend-service -n devsecops-arena`

Do you see any endpoints (IP addresses)? If not, the service isn't connected to the pod.
