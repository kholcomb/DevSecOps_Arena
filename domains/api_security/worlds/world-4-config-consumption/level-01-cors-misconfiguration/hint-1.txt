💡 Check the CORS headers. The API returns Access-Control-Allow-Origin reflecting your Origin header.
This means any website can make authenticated requests! Try:
curl -H 'Origin: https://evil.com' -H 'Authorization: Bearer token123' http://localhost:4006/api/user/profile
