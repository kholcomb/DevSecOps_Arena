---
# ServiceAccount for DevSecOps Arena players
apiVersion: v1
kind: ServiceAccount
metadata:
  name: devsecops-arena-player
  namespace: devsecops-arena

---
# Role with permissions only in devsecops-arena namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: devsecops-arena-player-role
  namespace: devsecops-arena
rules:
  # Allow all operations on common resources within devsecops-arena namespace
  - apiGroups: [""]
    resources:
      - pods
      - pods/log
      - pods/exec
      - services
      - configmaps
      - secrets
      - persistentvolumeclaims
      - events
    verbs: ["*"]
  
  # Allow all operations on apps resources
  - apiGroups: ["apps"]
    resources:
      - deployments
      - replicasets
      - statefulsets
      - daemonsets
    verbs: ["*"]
  
  # Allow all operations on batch resources
  - apiGroups: ["batch"]
    resources:
      - jobs
      - cronjobs
    verbs: ["*"]
  
  # Allow all operations on networking
  - apiGroups: ["networking.k8s.io"]
    resources:
      - ingresses
      - networkpolicies
    verbs: ["*"]
  
  # Read-only access to resource quotas and limit ranges
  - apiGroups: [""]
    resources:
      - resourcequotas
      - limitranges
    verbs: ["get", "list"]

---
# RoleBinding to grant permissions to the ServiceAccount
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: devsecops-arena-player-binding
  namespace: devsecops-arena
subjects:
  - kind: ServiceAccount
    name: devsecops-arena-player
    namespace: devsecops-arena
roleRef:
  kind: Role
  name: devsecops-arena-player-role
  apiGroup: rbac.authorization.k8s.io

---
# ClusterRole for minimal read-only cluster-wide access
# (allows viewing nodes, namespaces, etc. but NOT modifying)
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: devsecops-arena-viewer
rules:
  # View nodes (for understanding cluster)
  - apiGroups: [""]
    resources:
      - nodes
      - namespaces
    verbs: ["get", "list"]
  
  # View storage classes (for storage challenges)
  - apiGroups: ["storage.k8s.io"]
    resources:
      - storageclasses
    verbs: ["get", "list"]
  
  # View metrics (for observability challenges)
  - apiGroups: ["metrics.k8s.io"]
    resources:
      - nodes
      - pods
    verbs: ["get", "list"]

---
# ClusterRoleBinding for read-only cluster access
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: devsecops-arena-viewer-binding
subjects:
  - kind: ServiceAccount
    name: devsecops-arena-player
    namespace: devsecops-arena
roleRef:
  kind: ClusterRole
  name: devsecops-arena-viewer
  apiGroup: rbac.authorization.k8s.io
